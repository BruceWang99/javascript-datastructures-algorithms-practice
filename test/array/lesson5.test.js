import grayCode from "../../code/array/lesson5";
test("grayCode:2", () => {
  expect(grayCode(2)).toEqual([0, 1, 3, 2]);
});
// test("grayCode:3", () => {
//   expect(grayCode(3)).toEqual(["000", "001", "011", "010", "110", "111", "101", "100"]);
// });
